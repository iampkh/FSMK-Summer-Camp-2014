<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fsmkapp"
    android:versionCode="1"
    android:versionName="1.0"
    android:installLocation="auto" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="18" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.example.fsmkapp.MainActivity"
            android:label="@string/app_name" >
           
        </activity>
        <activity
            android:name="com.example.fsmkapp.DisplayDetails"
            android:label="@string/app_name" >
        </activity>

        <receiver android:name="com.example.fsmkapp.FsmkReceiver" >
        </receiver>
        <receiver android:name="com.example.fsmkapp.NotificationButtonReceiver" >
        </receiver>

        <service android:name="com.example.fsmkapp.AlarmMusicService" >
        </service>

        <activity
            android:name="com.example.fsmkapp.VolunteersActivity"
            android:label="@string/title_activity_volunteers" >
        </activity>
        <activity android:name="SplashScreen">
             <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="MenuActivity"></activity>
    </application>

</manifest>
